{% schema %}
{
  "name": "custom",
  "class": "index-section index-section--flush",
  "settings": [
    {
      "id": "placeholder_text",
      "type": "text",
      "label": "Placeholder Text",
      "default": "Search for products..."
    }
  ],
  "presets": [{
    "name": "custom",
  }]
}
{% endschema %}